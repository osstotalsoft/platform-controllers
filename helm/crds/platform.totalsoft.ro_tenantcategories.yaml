---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: tenantcategories.platform.totalsoft.ro
spec:
  group: platform.totalsoft.ro
  names:
    kind: TenantCategory
    listKind: TenantCategoryList
    plural: tenantcategories
    singular: tenantcategory
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.platformRef
      name: Platform
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          TenantCategory describes a classification that tenants can optionally reference
          (e.g. country, business typology, tenant group).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TenantCategorySpec is the spec for a tenant category.
            properties:
              description:
                type: string
              platformRef:
                description: |-
                  PlatformRef is the target platform. A TenantCategory is only resolvable by tenants
                  belonging to the same platform.
                type: string
              provisioningOverrides:
                description: |-
                  ProvisioningOverrides contains a list of resource overrides to be applied during provisioning,
                  for every tenant referencing this category.
                items:
                  description: ProvisioningResourceKind is a string value representing
                    the REST resource this dependency represents.
                  properties:
                    spec:
                      description: Spec contains the override spec for the resource.
                      x-kubernetes-preserve-unknown-fields: true
                    target:
                      description: Target contains the reference to the resource to
                        be patched.
                      properties:
                        apiVersion:
                          description: The API version of the dependency.
                          type: string
                        kind:
                          description: ' The kind of the dependency.'
                          type: string
                        name:
                          description: ' The name of the dependency.'
                          type: string
                        namespace:
                          description: ' The namespace of the dependency. If not specified,
                            the tenant namespace is assumed.'
                          type: string
                      required:
                      - apiVersion
                      - kind
                      - name
                      type: object
                  required:
                  - spec
                  - target
                  type: object
                type: array
            required:
            - platformRef
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
