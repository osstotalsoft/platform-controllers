---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: keycloakclients.provisioning.totalsoft.ro
spec:
  group: provisioning.totalsoft.ro
  names:
    kind: KeycloakClient
    listKind: KeycloakClientList
    plural: keycloakclients
    singular: keycloakclient
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clientName
      name: Keycloak client name
      type: string
    - jsonPath: .spec.platformRef
      name: Platform
      type: string
    - jsonPath: .spec.domainRef
      name: Domain
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              clientId:
                description: ClientName represents the client name.
                type: string
              clientName:
                description: ClientName represents the client name.
                type: string
              consentRequired:
                description: ConsentRequired indicates if consent is required.
                type: boolean
              defaultClientScopes:
                description: DefaultClientScopes is the list of default client scopes.
                items:
                  type: string
                type: array
              dependsOn:
                description: List of dependencies
                items:
                  properties:
                    kind:
                      description: Kind is a string value representing the REST resource
                        this dependency represents.
                      type: string
                    name:
                      description: ' The name of the dependency.'
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
              directAccessGrantsEnabled:
                description: DirectAccessGrantsEnabled enables direct access grants
                  (resource owner password).
                type: boolean
              domainRef:
                description: Business Domain that this resource is provision for.
                type: string
              enabled:
                description: Enabled indicates if the client is enabled.
                type: boolean
              exports:
                description: Export provisioning values spec.
                items:
                  properties:
                    clientId:
                      properties:
                        toConfigMap:
                          properties:
                            keyTemplate:
                              type: string
                          required:
                          - keyTemplate
                          type: object
                        toKubeSecret:
                          properties:
                            keyTemplate:
                              type: string
                          required:
                          - keyTemplate
                          type: object
                        toVault:
                          properties:
                            keyTemplate:
                              type: string
                          required:
                          - keyTemplate
                          type: object
                      type: object
                    clientSecret:
                      properties:
                        toConfigMap:
                          properties:
                            keyTemplate:
                              type: string
                          required:
                          - keyTemplate
                          type: object
                        toKubeSecret:
                          properties:
                            keyTemplate:
                              type: string
                          required:
                          - keyTemplate
                          type: object
                        toVault:
                          properties:
                            keyTemplate:
                              type: string
                          required:
                          - keyTemplate
                          type: object
                      type: object
                    domain:
                      description: The domain or bounded-context in which this client
                        will be used.
                      type: string
                  required:
                  - clientId
                  - clientSecret
                  - domain
                  type: object
                type: array
              fullScopeAllowed:
                description: FullScopeAllowed allows full scope.
                type: boolean
              implicitFlowEnabled:
                description: ImplicitFlowEnabled enables the implicit flow.
                type: boolean
              optionalClientScopes:
                description: OptionalClientScopes is the list of optional client scopes.
                items:
                  type: string
                type: array
              organization:
                description: Associated tenant name.
                type: string
              platformRef:
                description: Target platform (custom resource name).
                type: string
              protocol:
                description: Protocol e.g. "openid-connect"
                type: string
              protocolMappers:
                description: ProtocolMappers defines protocol mapper configurations.
                items:
                  description: ProtocolMapper represents a Keycloak protocol mapper
                    configuration.
                  properties:
                    config:
                      additionalProperties:
                        type: string
                      description: Config is a key-value map of mapper configuration.
                      type: object
                    name:
                      description: Name of the protocol mapper.
                      type: string
                    protocol:
                      description: Protocol e.g. "openid-connect"
                      type: string
                    protocolMapper:
                      description: ProtocolMapper is the mapper type e.g. "oidc-audience-mapper"
                      type: string
                  required:
                  - name
                  - protocol
                  - protocolMapper
                  type: object
                type: array
              publicClient:
                description: PublicClient indicates if this is a public client.
                type: boolean
              realm:
                description: Associated realm.
                type: string
              serviceAccountsEnabled:
                description: ServiceAccountsEnabled enables service accounts.
                type: boolean
              standardFlowEnabled:
                description: StandardFlowEnabled enables the standard (authorization
                  code) flow.
                type: boolean
              target:
                default:
                  category: Tenant
                description: The provisioning target.
                properties:
                  category:
                    default: Tenant
                    description: 'Provisioning target type. Possible values: Tenant,
                      Platform'
                    enum:
                    - Tenant
                    - Platform
                    type: string
                  filter:
                    description: |-
                      Filter targets (applies for category "Tenant").
                      If ommited all targets are selected.
                    properties:
                      kind:
                        default: Blacklist
                        description: 'Includes or excludes the speciffied targets.
                          Possibile values: Blacklist, Whitelist'
                        enum:
                        - Blacklist
                        - Whitelist
                        type: string
                      values:
                        description: A list of targets to include or exculde
                        items:
                          type: string
                        type: array
                    required:
                    - kind
                    type: object
                required:
                - category
                type: object
              tenantOverrides:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                description: |-
                  Overrides for tenants. Dictionary with tenant name as key, spec override as value.
                  The spec override has the same structure as Spec
                type: object
            required:
            - clientId
            - clientName
            - consentRequired
            - directAccessGrantsEnabled
            - domainRef
            - enabled
            - fullScopeAllowed
            - implicitFlowEnabled
            - platformRef
            - publicClient
            - realm
            - serviceAccountsEnabled
            - standardFlowEnabled
            - target
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
